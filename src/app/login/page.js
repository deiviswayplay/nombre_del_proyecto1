 import LoginForm from "../components/login-form"
import loginForm  from "../components/login-form"
 
 export default function Page ( ) {
    const loginUser = (user)=>{
    }
    
    return (
        <div>
          
            <LoginForm />
        </div>
    )
}